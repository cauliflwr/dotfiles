---
- name: "Nextcloud | Configuration"
  tags: configure, never
  block:
    # https://formulae.brew.sh/cask/nextcloud
    - name: "Nextcloud | Install"
      community.general.homebrew_cask:
        name: nextcloud
        state: present
